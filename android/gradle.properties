# AndroidX configuration
android.useAndroidX=true

# Suppress build warnings for compileSdk 35 (Android 15)
android.suppressUnsupportedCompileSdk=35

# Jetifier for library migration
android.enableJetifier=true

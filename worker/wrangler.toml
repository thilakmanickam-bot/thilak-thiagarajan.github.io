name = "halo-chat-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets are NOT set here — configure them out-of-band so they never touch the
# repo (see README.md):
#   wrangler secret put ANTHROPIC_API_KEY
#   wrangler secret put APP_TOKEN
